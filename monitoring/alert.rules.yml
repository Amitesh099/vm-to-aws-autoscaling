groups:
- name: cpu-alert
  rules:
  - alert: HighCPU
    expr: 100 - (avg by(instance)(irate(node_cpu_seconds_total{mode="idle"}[1m])) * 100) > 75
    for: 10s
    labels:
      severity: critical
    annotations:
      summary: "High CPU detected"
